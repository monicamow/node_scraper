A Node.js Webscraper by Monica Mow
===================================

This JavaScript program scrapes HTML elements from substack.net/images/ and saves the data to a CSV file. It uses the npm modules `cheerio` and `request`.